<resources
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="TradingApp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/bg_primary</item>
        <item name="colorPrimaryVariant">@color/bg_primary_variant_dark</item>
        <item name="colorOnPrimary">@color/bg_primary_variant_bright</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/bg_secondary</item>
        <item name="colorSecondaryVariant">@color/bg_secondary_variant_bright</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/bg_primary</item>
        <!-- Customize your theme here. -->
    </style>
</resources>